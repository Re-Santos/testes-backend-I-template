// crie uma validação que recebe uma string como parâmetro e retorna um array de strings onde cada item
// é uma letra da palavra original
//Exemplo: a entrada "dev" gera a saída ["d", "e", "v"]
// Crie pelo menos um teste que valida sua implementação
export const fixacao = (str: string): string[] => {
    return str.split ("")
}